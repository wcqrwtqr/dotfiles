# Set prefix to Ctrl-Space instead of Ctrl-b
# unbind C-b
# set -g prefix C-Space  old method of using ctl + space
# bind Space send-prefix   old binding the ctl + pase

set-option -g prefix C-a
unbind-key C-a
bind-key C-a send-prefix


# Split windows using | and -
unbind '"'
unbind %
bind | split-window -h
bind - split-window -v


# mouse setting
setw -g mouse on



# set -g status-style fg=colour244
# set -g pane-active-border-style fg=colour250

# set -g default-terminal "screen-256color"

# set-window-option -g window-status-current-style fg=colour223

#==============
# this cold below to find a solution for vim not showing colors while running tmux
#==============


# Bind the direction of navigation to use the hjkl
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R



